# twindex
Tool to detect duplicate files (more copies of the same file on different locations).

## Prerequisites

**Install wxPython**

Windows:
```
pip install wxpython
```

Linux:
```
apt install python3-wxgtk4.0
```
